      // console.log(messageResponse.data.raw)
        // let encodedMailBody = messageResponse.data.payload.body.data ? messageResponse.data.payload.body.data.replace(/-/g, '+')?.replace(/_/g, '/') : "";
        // let decodedMailBody = [];
        // let decodedMailBody = decodeEmailContent(messageResponse.data.raw)
        // console.log(decodedMailBody)
        // setEmailContent(p=>[...p,decodedMailBody]);

// const emailParts = messageResponse.data.payload.parts;
        // console.log(emailParts, messageResponse.data.payload.body.data)
        // if(emailParts){for (const part of emailParts) {
        //   if (part.mimeType === 'text/html') {
        //     const encodedContent = part.body.data;
        //     const decodedHtmlContent = decodeEmailContent(encodedContent);
        //     decodedMailBody.push(decodedHtmlContent)
        //     setEmailContent(p=>[...p,decodedHtmlContent]);
        //     // break;
        //   }
        //   else if (part.mimeType === 'text/plain'){
        //     decodedMailBody.push(decodeEmailContent(part.body.data))
        //   }
        // }}else{
        //   encodedMailBody = messageResponse.data.payload.body.data ? messageResponse.data.payload.body.data.replace(/-/g, '+')?.replace(/_/g, '/') : "";
        //   decodedMailBody.push = atob(encodedMailBody);
        // }